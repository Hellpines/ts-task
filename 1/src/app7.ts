// 7. Напишите программу, которая принимает строку str и переставляет местами первый и последний
// символы строки. Выведите результат в консоль.
// Входные: str = "hello" → Результат: "oellh"
// Входные: str = "typescript" → Результат: "tpescripy"

// function Reversed(str : string) : string{
//     let result ="";
//     result = str[str.length-1] + str.slice(1, str.length - 1) + str[0];
//     return result
// }
// console.log(Reversed("hello"));

